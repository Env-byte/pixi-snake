import {MainController} from "./controllers";
import {Box} from "@mui/material";

export const App = () => {
    return <Box sx={{height: '100vh', background: '#645394'}}>
        <MainController/>
    </Box>
}